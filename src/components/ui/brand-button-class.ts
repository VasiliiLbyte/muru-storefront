/**
 * Размер брендовой кнопки.
 *
 * Десктоп: 32px с текстом 14px. Ровнять коробку кнопки с логотипом (оба 24px)
 * пробовали — оптически не сходится: у логотипа буквы заполняют все 24px,
 * а у кнопки внутри той же высоты лежит текст с полями, и она читается
 * мельче. 32px уравновешивают их на глаз.
 * Мобайл: 44px — минимум для пальца и гейт `e2e/mobile-tap-targets.spec.ts`.
 *
 * Начертание тоже здесь: базовый вариант `Button` приходит с `font-medium`,
 * а по макету все кнопки светлые (300). Держим в одном месте, чтобы кнопки
 * не расходились по страницам.
 */
export const brandButtonSize =
  "h-11 px-8 text-[14px] leading-[17px] font-light lg:h-8 lg:text-[14px]";
